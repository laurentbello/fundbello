# Last monitor run
# The monitor reads the most recent date here to set its lookback window ("developments
# since <date>"), then overwrites this file at the end of each run. Seeded below so the
# first run looks back a sensible window instead of all-time.
last_run: 2026-07-31
mode: weekly
verdicts: 15
digest: portfolio/out/digest.json (composed, SMTP send failed again — GMAIL_ADDRESS/GMAIL_APP_PASSWORD still not set — delivered as Gmail draft fallback, id r-8030220157294869969. 13/15 names reported H1/Q2 this window. 5 STRENGTHEN signals fired (SAF, AIR, SPGI, MCO, WM — all pre-registered ADD; WM's ADD overridden by its >$230 re-underwrite price trigger, net HOLD). 0 URGENT, 0 confirmed BREAKs. VINCI Airports Q2 pax -1.3% flagged WATCH but does NOT meet the 2-consecutive-quarter BREAK-2 bar (Q1 was +1.5%) — watch Q3. LIN Q2 earnings release later same day (10:00 UTC), not yet out at scan time. Cluster read-through: SPGI+MCO shared debt-issuance reacceleration (positive, no break). Consider fixing GMAIL_ADDRESS/GMAIL_APP_PASSWORD env vars so SMTP stops failing every cycle.)
