# Last monitor run
# The monitor reads the most recent date here to set its lookback window ("developments
# since <date>"), then overwrites this file at the end of each run. Seeded below so the
# first run looks back a sensible window instead of all-time.
last_run: 2026-07-02
mode: weekly
verdicts: 15
digest: no email sent — all-clear week (2 WATCH: AENA DORA III tariff-cut recommendation; Semis cluster MATCH Act export-control bill), no HARD catalyst landed in lookback window
