# Last monitor run
# The monitor reads the most recent date here to set its lookback window ("developments
# since <date>"), then overwrites this file at the end of each run. Seeded below so the
# first run looks back a sensible window instead of all-time.
last_run: 2026-07-14
mode: weekly
verdicts: 15
digest: portfolio/out/digest.json (composed; 4 ALERT(S) — AENA/ASML/WM on WATCH + payment-networks (V+MA) cluster read-through on the FY2027 NDAA/CCCA vehicle now live on Senate floor; WM's $230 re-underwrite price trigger fired; none URGENT, no BREAK/STRENGTHEN fired, no cluster break test tripped; earnings dates firmed for MCO(07-22)/SPGI(07-28)/V(07-28)/MA(07-30)/LRCX(07-29); see catalyst_calendar.md for details)
